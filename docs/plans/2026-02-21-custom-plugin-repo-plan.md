# Custom Plugin Repository + Taskwarrior Plugin — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ein eigenes Noctalia-Plugin-Repository auf GitHub erstellen (`hnsstrk/noctalia-custom-plugins`) und das Taskwarrior-Plugin dort veröffentlichen.

**Architecture:** Das Repository folgt exakt dem Pattern des offiziellen `noctalia-dev/noctalia-plugins` Repos: Plugin-Verzeichnisse auf Root-Ebene, `registry.json` als zentraler Index (auto-generiert via GitHub Actions), `update-registry.js` als Generator-Script. Das Taskwarrior-Plugin wird nach dem bestehenden IMPLEMENTATION.md Schritt für Schritt implementiert.

**Tech Stack:** Git/GitHub, GitHub Actions, Node.js (Registry-Script), QML/JavaScript (Quickshell Framework), Taskwarrior CLI, Bash (Hook-Script)

**Design Document:** Siehe `docs/plans/2026-02-21-custom-plugin-repo-design.md`

---

## Phase 1: Repository-Infrastruktur

### Task 1: Git-Repository initialisieren

**Files:** Create: `noctalia-custom-plugins/` (lokales Git-Repo)

**Step 1:** Lokales Git-Repository initialisieren.

```bash
cd /home/hnsstrk/Repositories/noctalia-custom-plugins
git init
```

**Step 2:** GitHub-Repository erstellen und als Remote hinzufügen.

```bash
gh repo create hnsstrk/noctalia-custom-plugins --public --description "Custom Noctalia Shell plugins by hnsstrk" --source . --push
```

**Step 3:** Verifikation: `gh repo view hnsstrk/noctalia-custom-plugins` — Repo muss existieren.

---

### Task 2: README.md erstellen

**Files:** Create: `README.md`

**Step 1:** README mit Repo-Beschreibung, Plugin-Liste und Installationsanleitung erstellen.

```markdown
# Noctalia Custom Plugins

Custom plugins for [Noctalia Shell](https://github.com/noctalia-dev/noctalia-shell) by hnsstrk.

## Installation

1. Open Noctalia Settings → Plugins → Sources
2. Click "+ Add custom repository"
3. Enter URL: `https://github.com/hnsstrk/noctalia-custom-plugins`
4. Plugins will appear in the "Available" tab

## Plugins

| Plugin | Description | Version |
|--------|-------------|---------|
| [Taskwarrior](taskwarrior/) | A full-featured Taskwarrior client | 1.0.0 |

## Development

```bash
# Update registry manually
node .github/workflows/update-registry.js
```

## License

MIT — See individual plugin licenses in their respective directories.
```

**Step 2:** Commit: `git add README.md && git commit -m "docs: add README with installation instructions"`

---

### Task 3: CLAUDE.md erstellen

**Files:** Create: `CLAUDE.md`

**Step 1:** CLAUDE.md mit Projektkontext für Claude Code.

```markdown
# CLAUDE.md

## Project Overview

Custom plugin repository for **Noctalia Shell** — a Linux desktop shell for Wayland compositors. Contains plugins written in **QML/JavaScript** using the **Quickshell** framework.

## Commands

```bash
# Update registry.json from all manifest.json files
node .github/workflows/update-registry.js
```

No build step, test suite, or linter. Plugins are QML files loaded directly by Noctalia Shell at runtime.

## Architecture

### Plugin Layout

Each plugin is a top-level directory containing:

- **`manifest.json`** (required) — plugin metadata, version, entry points, default settings
- **`Main.qml`** — IPC handler and core logic; receives `pluginApi` property
- **`BarWidget.qml`** — status bar icon/widget
- **`Panel.qml`** — popup panel; exposes `contentPreferredWidth/Height` and `allowAttach`
- **`Settings.qml`** — settings UI with `saveSettings()` function
- **`i18n/*.json`** — translations; `en.json` is the reference
- **`README.md`** — plugin documentation

### Registry

`registry.json` is auto-generated by GitHub Actions. Never edit manually.

## Conventions

### Git Commits

Format: `type(scope): message`
Types: `feat`, `fix`, `chore`, `i18n`, `docs`

### i18n

- `en.json` is the source of truth
- Placeholders `{var}` must be preserved in translations
- Key suffixes: `-label`, `-description`, `-placeholder`, `-tooltip`
```

**Step 2:** Commit: `git add CLAUDE.md && git commit -m "chore: add CLAUDE.md for Claude Code context"`

---

### Task 4: GitHub Actions Workflow erstellen

**Files:**
- Create: `.github/workflows/update-registry.js`
- Create: `.github/workflows/update-registry.yml`

**Step 1:** `update-registry.js` vom offiziellen Repo übernehmen und anpassen. Das Script scannt alle Top-Level-Verzeichnisse nach `manifest.json` und generiert `registry.json`.

Das Script ist identisch zum offiziellen Repo unter `/home/hnsstrk/Repositories/noctalia-plugins/.github/workflows/update-registry.js` — 1:1 kopieren.

**Step 2:** `update-registry.yml` erstellen:

```yaml
name: Update Plugin Registry
on:
  push:
    branches:
      - main
    paths:
      - '**/manifest.json'
      - '.github/workflows/update-registry.js'
  workflow_dispatch:

jobs:
  update-registry:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run registry update script
        run: node .github/workflows/update-registry.js

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet registry.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add registry.json
          git commit -m "chore: update plugin registry [skip ci]"
          git push
```

**Step 3:** Commit: `git add .github/ && git commit -m "ci: add GitHub Actions workflow for registry auto-update"`

---

### Task 5: PLAN.md und IMPLEMENTATION.md verschieben

**Files:**
- Move: `PLAN.md` → `docs/PLAN.md`
- Move: `IMPLEMENTATION.md` → `docs/IMPLEMENTATION.md`

**Step 1:** Dateien aus dem noctalia-plugins Repo kopieren (nicht per git mv, da anderes Repo).

```bash
cp /home/hnsstrk/Repositories/noctalia-plugins/PLAN.md /home/hnsstrk/Repositories/noctalia-custom-plugins/docs/PLAN.md
cp /home/hnsstrk/Repositories/noctalia-plugins/IMPLEMENTATION.md /home/hnsstrk/Repositories/noctalia-custom-plugins/docs/IMPLEMENTATION.md
```

**Step 2:** In PLAN.md und IMPLEMENTATION.md die `repository`-URL in der manifest.json-Referenz anpassen:

```
"repository": "https://github.com/hnsstrk/noctalia-custom-plugins"
```

**Step 3:** Commit: `git add docs/ && git commit -m "docs: add Taskwarrior design document and implementation plan"`

---

### Task 6: Leere registry.json erstellen und initialen Push machen

**Files:** Create: `registry.json`

**Step 1:** Initiale leere Registry erstellen:

```json
{
  "version": 1,
  "plugins": []
}
```

**Step 2:** Commit und Push:

```bash
git add registry.json
git commit -m "chore: add initial empty registry.json"
git push -u origin main
```

**Step 3:** Verifikation: `gh repo view hnsstrk/noctalia-custom-plugins --web` — Repo mit allen Dateien sichtbar.

---

## Phase 2: Taskwarrior-Plugin implementieren

Die folgenden Tasks 7-23 entsprechen den Tasks 1-17 aus `docs/IMPLEMENTATION.md`. Der vollständige Code und die Verifikationsschritte stehen dort.

**Hinweis:** Für die detaillierten Code-Listings und Verifikationsschritte siehe `docs/IMPLEMENTATION.md`. Hier nur die Übersicht:

### Task 7: manifest.json erstellen
**Files:** Create: `taskwarrior/manifest.json`
→ Entspricht IMPLEMENTATION.md Task 1

### Task 8: Main.qml — Basis-Struktur
**Files:** Create: `taskwarrior/Main.qml`
→ Entspricht IMPLEMENTATION.md Task 2

### Task 9: Main.qml — Filter-Engine
**Files:** Edit: `taskwarrior/Main.qml`
→ Entspricht IMPLEMENTATION.md Task 3

### Task 10: Main.qml — CRUD Aktionen
**Files:** Edit: `taskwarrior/Main.qml`
→ Entspricht IMPLEMENTATION.md Task 4

### Task 11: Main.qml — Metadaten laden
**Files:** Edit: `taskwarrior/Main.qml`
→ Entspricht IMPLEMENTATION.md Task 5

### Task 12: BarWidget.qml
**Files:** Create: `taskwarrior/BarWidget.qml`
→ Entspricht IMPLEMENTATION.md Task 6

### Task 13: Panel.qml — Basis-Layout
**Files:** Create: `taskwarrior/Panel.qml`
→ Entspricht IMPLEMENTATION.md Task 7

### Task 14: Panel.qml — Filter-Bar
**Files:** Edit: `taskwarrior/Panel.qml`
→ Entspricht IMPLEMENTATION.md Task 8

### Task 15: Panel.qml — Task-Eingabe
**Files:** Edit: `taskwarrior/Panel.qml`
→ Entspricht IMPLEMENTATION.md Task 9

### Task 16: Panel.qml — Task-Liste
**Files:** Edit: `taskwarrior/Panel.qml`
→ Entspricht IMPLEMENTATION.md Task 10

### Task 17: Panel.qml — Detail-Dialog
**Files:** Edit: `taskwarrior/Panel.qml`
→ Entspricht IMPLEMENTATION.md Task 11

### Task 18: Panel.qml — Delete-Bestätigungs-Dialog
**Files:** Edit: `taskwarrior/Panel.qml`
→ Entspricht IMPLEMENTATION.md Task 12

### Task 19: Settings.qml
**Files:** Create: `taskwarrior/Settings.qml`
→ Entspricht IMPLEMENTATION.md Task 13

### Task 20: Hook-Script und Install/Remove Logik
**Files:** Create: `taskwarrior/hooks/on-exit-noctalia`, Edit: `taskwarrior/Main.qml`
→ Entspricht IMPLEMENTATION.md Task 14

### Task 21: i18n en.json
**Files:** Create: `taskwarrior/i18n/en.json`
→ Entspricht IMPLEMENTATION.md Task 15

### Task 22: Registry Update
**Files:** Auto-generated: `registry.json`

**Step 1:** Registry lokal generieren:
```bash
node .github/workflows/update-registry.js
```

**Step 2:** Verifikation: `grep -A 5 '"taskwarrior"' registry.json`

**Step 3:** Commit und Push:
```bash
git add registry.json
git commit -m "chore(taskwarrior): update registry for v1.0.0"
git push
```

### Task 23: Finale Verifikation
→ Entspricht IMPLEMENTATION.md Task 17 + In Noctalia Shell einbinden und testen.

---

## Phase 3: Veröffentlichung und Logging

### Task 24: In Noctalia Shell einbinden

**Step 1:** Noctalia Shell öffnen → Plugins → Quellen
**Step 2:** "+ Benutzerdefiniertes Repository hinzufügen"
**Step 3:** URL eingeben: `https://github.com/hnsstrk/noctalia-custom-plugins`
**Step 4:** Zum "Verfügbar"-Tab wechseln → Taskwarrior-Plugin installieren
**Step 5:** Plugin aktivieren und testen

### Task 25: Obsidian-Logging

**Step 1:** Journal-Eintrag in `Journal/Daily/2026-02-21.md`
**Step 2:** Dokumentation in `Notes/` erstellen (falls nötig)
